#include <stdio.h>

int main() {
    int num1, num2, sum;

    // User se input lena
    printf("Enter the number num1: ");
    scanf("%d", &num1);

    printf("Enter the number num2: ");
    scanf("%d", &num2);

    // Dono ka sum
    sum = num1 + num2;

    // Result dikhana
    printf("the value is sum: %d\n", sum);

    return 0;
}
